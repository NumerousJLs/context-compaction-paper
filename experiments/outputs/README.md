Saved run outputs live here.

Each run should create its own timestamped directory containing:

- request metadata
- prompt text
- raw response
- parsed response
- summary metrics
- errors if any

This directory is git-ignored except for this file.
