Recover the current coding-session state from the context below.
Use only the provided session context and recent turns.
Choose exactly one option ID for each field.
Return JSON only with these keys: goal_id, file_id, constraint_id, detail_id, next_step_id.

COMPACTED MEMORY:
- Goal: Finish the billing webhook rollout without repeating already completed setup work.
- Progress: Schema migration, secret loading, and endpoint registration are already complete.
- The latest correction changed the work plan, so keep following the newest rule: the schema work is already done.
- The open issue is Only the 409 retry behavior is still failing.
- There is still one brittle exact detail from earlier notes that matters.
- Next: continue the pending fix and rerun the relevant verification.

RECENT TURNS:
Turn 1 [User]: I found a stale reminder from an abandoned branch. It says to work in `billing/webhook_schema.py` and to use this old direction: Revisit the schema migration before touching runtime logic.
Turn 2 [Tool]: Stale reminder detail that may be wrong: The remaining issue is a schema migration hash mismatch.
Turn 3 [Assistant]: That note looks stale. I should not trust the reminder file, rule, detail, or next step until I reconcile it with the earlier session.
Turn 4 [User]: Exactly. Recover the real final state from the earlier context, not the stale reminder, before you continue.
Turn 5 [Assistant]: I will ignore the stale reminder unless the earlier context confirms this old next step: Re-edit `billing/webhook_schema.py` even though the migration is already done.

GOAL OPTIONS:
  A: Build a billing analytics dashboard first.
  B: Finish the billing webhook rollout without repeating already completed setup work.
  C: Replace billing webhooks with a polling job.

FILE OPTIONS:
  A: billing/webhook_consumer.py
  B: billing/webhook_schema.py
  C: tests/test_webhook_consumer.py

CONSTRAINT OPTIONS:
  A: Revisit the schema migration before touching runtime logic.
  B: Pause rollout and redesign webhook payloads.
  C: Do not touch the already-migrated schema file.

DETAIL OPTIONS:
  A: The remaining failing test is `tests/test_webhook_consumer.py::test_retries_409`.
  B: The remaining issue is a schema migration hash mismatch.
  C: The real problem is a chart legend in the billing UI.

NEXT_STEP OPTIONS:
  A: Re-edit `billing/webhook_schema.py` even though the migration is already done.
  B: Ignore the backend and build a billing analytics dashboard.
  C: Edit `billing/webhook_consumer.py` to handle the 409 retry path and rerun `pytest tests/test_webhook_consumer.py -q`.

Return strict JSON only.