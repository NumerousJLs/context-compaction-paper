Recover the current coding-session state from the context below.
Use only the provided session context and recent turns.
Choose exactly one option ID for each field.
Return JSON only with these keys: goal_id, file_id, constraint_id, detail_id, next_step_id.

EARLIER SESSION:
Turn 1 [User]: We need to finish the search cli flag regression. Restore the search reindex CLI so targeted reindexing works again.
Turn 2 [Assistant]: My first pass was to work in `search/indexer.py` and follow this rule: Always force a full reindex to avoid edge cases.
Turn 3 [Tool]: Status note: The job queue submission and index existence check already work.
Turn 4 [User]: Do not lose the key detail from this task: The exact flag is `--index users_v2`.
Turn 5 [Assistant]: Understood. The remaining blocker seems to be: The CLI dropped the exact target index flag.
Turn 6 [User]: Correction: the active file is actually `cli/reindex_search.py`. Also, the latest rule is: Keep incremental reindexing; do not force full reindex on every run.
Turn 7 [Assistant]: Thanks. I will update the plan around `cli/reindex_search.py` and keep in mind that the exact brittle detail matters more than the earlier rough description.
Turn 8 [Tool]: Open issue recap: The CLI dropped the exact target index flag.

RECENT TURNS:
Turn 1 [User]: We compacted the earlier session because the chat was getting long.
Turn 2 [Assistant]: I can resume once I recover the active task state from the earlier context.
Turn 3 [User]: Please recover the current state exactly before taking the next step.

GOAL OPTIONS:
  A: Restore the search reindex CLI so targeted reindexing works again.
  B: Rebuild search as a hosted service first.
  C: Add a UI for reindex history.

FILE OPTIONS:
  A: search/indexer.py
  B: cli/reindex_search.py
  C: tests/test_reindex_search.py

CONSTRAINT OPTIONS:
  A: Always force a full reindex to avoid edge cases.
  B: Move reindexing into the frontend admin panel.
  C: Keep incremental reindexing; do not force full reindex on every run.

DETAIL OPTIONS:
  A: The old flag was `--index users_legacy`.
  B: The exact flag is `--index users_v2`.
  C: The issue is a font mismatch in the search admin page.

NEXT_STEP OPTIONS:
  A: Edit `cli/reindex_search.py` to preserve incremental mode and keep `--index users_v2`, then rerun `pytest tests/test_reindex_search.py -q`.
  B: Edit `search/indexer.py` and force full reindex on `users_legacy`.
  C: Ignore the CLI and build a reindex history UI.

Return strict JSON only.