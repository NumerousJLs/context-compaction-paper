Recover the current coding-session state from the context below.
Use only the provided session context and recent turns.
Choose exactly one option ID for each field.
Return JSON only with these keys: goal_id, file_id, constraint_id, detail_id, next_step_id.

COMPACTED MEMORY:
- Goal: Finish the theme asset cache refresh so new logos appear after publish.
- Progress: Uploads, manifest writes, and publish hooks are already complete.
- Current focus is in the theme asset refresh job, and the latest rule is uploads are already done.
- The open issue is Only cache invalidation for the new logo still fails.
- Next: finish the pending fix and preserve the stale logo_dark cache key.

RECENT TURNS:
Turn 1 [User]: We compacted the earlier session because the chat was getting long.
Turn 2 [Assistant]: I can resume once I recover the active task state from the earlier context.
Turn 3 [User]: Please recover the current state exactly before taking the next step.

GOAL OPTIONS:
  A: Rebuild the entire theming system first.
  B: Create a theme preview gallery.
  C: Finish the theme asset cache refresh so new logos appear after publish.

FILE OPTIONS:
  A: themes/refresh_assets.py
  B: themes/upload_assets.py
  C: tests/test_refresh_assets.py

CONSTRAINT OPTIONS:
  A: Redo uploads before touching refresh invalidation.
  B: Uploads already work; do not revisit the upload path.
  C: Stop the refresh job and focus on a theme gallery.

DETAIL OPTIONS:
  A: The remaining issue is the stale cache key for `logo_dark.svg`.
  B: The remaining issue is a missing upload form field.
  C: The actual problem is a layout mismatch in the gallery.

NEXT_STEP OPTIONS:
  A: Re-edit `themes/upload_assets.py` even though uploads already work.
  B: Ignore the refresh job and build a theme preview gallery.
  C: Edit `themes/refresh_assets.py` to invalidate the stale `logo_dark.svg` cache key and rerun `pytest tests/test_refresh_assets.py -q`.

Return strict JSON only.