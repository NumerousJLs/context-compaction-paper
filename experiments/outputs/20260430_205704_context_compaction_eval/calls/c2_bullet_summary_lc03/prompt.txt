Recover the current coding-session state from the context below.
Use only the provided session context and recent turns.
Choose exactly one option ID for each field.
Return JSON only with these keys: goal_id, file_id, constraint_id, detail_id, next_step_id.

COMPACTED MEMORY:
- Goal: Fix webhook signature verification so billing callbacks stop failing in staging.
- Progress: Route registration and secret loading already work.
- Current focus is in the webhook signature middleware, and the latest rule is the raw-bytes verification rule.
- The open issue is The signature check still uses the wrong payload representation.
- Next: finish the pending fix and preserve the timestamp signature header.

RECENT TURNS:
Turn 1 [User]: We compacted the earlier session because the chat was getting long.
Turn 2 [Assistant]: I can resume once I recover the active task state from the earlier context.
Turn 3 [User]: Please recover the current state exactly before taking the next step.

GOAL OPTIONS:
  A: Create a new billing dashboard tab for webhook history.
  B: Rotate all production secrets and redeploy the entire stack.
  C: Fix webhook signature verification so billing callbacks stop failing in staging.

FILE OPTIONS:
  A: middleware/webhook_signature.py
  B: routes/webhooks.py
  C: tests/test_webhook_signature.py

CONSTRAINT OPTIONS:
  A: Keep hashing the parsed JSON string because it is easier to debug.
  B: Use the raw request body bytes; do not verify against the parsed JSON string.
  C: Move verification entirely into the frontend dev proxy.

DETAIL OPTIONS:
  A: The exact header to keep is `X-Signature-Timestamp`.
  B: The only header that matters is `X-Request-Id`.
  C: The main bug is a CSS issue in the staging admin page.

NEXT_STEP OPTIONS:
  A: Edit `routes/webhooks.py` and keep verifying the parsed JSON string.
  B: Skip verification and work on the billing dashboard tab.
  C: Edit `middleware/webhook_signature.py` to verify the raw body bytes and preserve `X-Signature-Timestamp`, then rerun `pytest tests/test_webhook_signature.py -q`.

Return strict JSON only.