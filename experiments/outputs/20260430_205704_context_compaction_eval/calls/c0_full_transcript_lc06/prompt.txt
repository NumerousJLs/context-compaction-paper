Recover the current coding-session state from the context below.
Use only the provided session context and recent turns.
Choose exactly one option ID for each field.
Return JSON only with these keys: goal_id, file_id, constraint_id, detail_id, next_step_id.

EARLIER SESSION:
Turn 1 [User]: We need to finish the authentication callback handler. Repair the OAuth callback handler so the login redirect flow finishes correctly.
Turn 2 [Assistant]: My first pass was to work in `auth/session_store.py` and follow this rule: Exchange the auth code first and validate `state` later.
Turn 3 [Tool]: Status note: Session persistence and redirect mapping already work.
Turn 4 [User]: Do not lose the key detail from this task: The exact redirect path must remain `/auth/callback`.
Turn 5 [Assistant]: Understood. The remaining blocker seems to be: The callback still applies the wrong ordering for state validation.
Turn 6 [User]: Correction: the active file is actually `auth/callback_handler.py`. Also, the latest rule is: Validate the `state` parameter before exchanging the auth code.
Turn 7 [Assistant]: Thanks. I will update the plan around `auth/callback_handler.py` and keep in mind that the latest correction overrides the earlier plan.
Turn 8 [Tool]: Open issue recap: The callback still applies the wrong ordering for state validation.

RECENT TURNS:
Turn 1 [User]: We compacted the earlier session because the chat was getting long.
Turn 2 [Assistant]: I can resume once I recover the active task state from the earlier context.
Turn 3 [User]: Please recover the current state exactly before taking the next step.

GOAL OPTIONS:
  A: Build a new login screen animation for the app.
  B: Replace OAuth with SAML before fixing the callback.
  C: Repair the OAuth callback handler so the login redirect flow finishes correctly.

FILE OPTIONS:
  A: auth/session_store.py
  B: auth/callback_handler.py
  C: tests/test_callback_handler.py

CONSTRAINT OPTIONS:
  A: Validate the `state` parameter before exchanging the auth code.
  B: Exchange the auth code first and validate `state` later.
  C: Skip state validation in staging for easier testing.

DETAIL OPTIONS:
  A: The only path that matters is `/auth/login`.
  B: The exact redirect path must remain `/auth/callback`.
  C: The problem is a spacing issue in the login button text.

NEXT_STEP OPTIONS:
  A: Edit `auth/callback_handler.py` to validate `state` before code exchange and keep `/auth/callback`, then rerun `pytest tests/test_callback_handler.py -q`.
  B: Edit `auth/session_store.py` and keep validating `state` later.
  C: Skip the backend and animate the login screen.

Return strict JSON only.